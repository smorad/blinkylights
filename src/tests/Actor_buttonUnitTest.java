public class Actor_buttonUnitTest {
  Actor_button test;
  
  // Initializes test button with "BLUE" as the string
  test.initialize_actor_button (BLUE);
  
  // Sets position of test at x = 20, y = 20
  test.set_position(20, 20);
  
  // Overrides draw
  test.draw (batch, alpha);
}